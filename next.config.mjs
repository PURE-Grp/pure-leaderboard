/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static export — outputs pure HTML/JS/CSS to /out
  // Deploy to any static host: Cloudflare Pages, GitHub Pages, Netlify, S3, etc.
  output: "export",
};
export default nextConfig;
